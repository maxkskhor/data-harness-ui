[project]
name = "backend"
version = "0.1.0"
description = "Local FastAPI backend for data-harness-ui."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "data-harness[openai]==0.3.0",
    "fastapi>=0.115.0",
    "pandas>=2.2.0",
    "python-dotenv>=1.0.0",
    "python-multipart>=0.0.20",
    "uvicorn[standard]>=0.32.0",
]

[dependency-groups]
dev = [
    "httpx>=0.27.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
